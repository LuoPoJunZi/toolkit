# 运行级自检清单（当前主菜单）

本文用于在真实 VPS 环境中逐菜单验证功能可用性，避免“看起来有菜单、实际空执行”。

## 1. 自检前准备

- [ ] 使用 `root` 用户登录
- [ ] 更新到最新脚本：`bash <(curl -fsSL https://z.evzzz.com)`
- [ ] 启动工具箱：`z`
- [ ] 记录日志路径：
  - 操作日志：`/opt/luopo-toolkit/logs/action.log`
  - 错误日志：`/opt/luopo-toolkit/logs/error.log`

## 2. 通用通过标准

- [ ] 每个功能必须有明确执行结果（成功或失败提示）
- [ ] 不允许只出现“按回车继续...”且没有过程输出
- [ ] 失败时需有可理解的错误提示（依赖缺失/下载失败/执行失败）
- [ ] 高风险操作需有确认提示（如重启、卸载、批量操作）

## 3. 主菜单逐项检查

### 1. 系统信息查询

- [ ] 输出系统/资源/网络分区信息
- [ ] 无 `numfmt` 报错
- [ ] 核心字段不为空（主机名、内核、内存、磁盘、IPv4）

### 2. 系统全面更新

- [ ] 有更新过程输出
- [ ] 有明确完成状态（成功/失败）

### 3. 系统垃圾清理

- [ ] 有清理过程输出
- [ ] 有明确完成状态（成功/失败）

### 4. 一键脚本中心

- [ ] 脚本列表完整显示
- [ ] 任意脚本可真正执行，不是秒退
- [ ] 下载失败时有明确错误提示

### 5. 基础工具

- [ ] 状态表格正常显示，已安装/未安装工具可区分
- [ ] 任意 2 个安装项可执行，例如 `curl`、`tmux`
- [ ] 自定义安装/卸载输入无效项时有提示

### 6. BBR管理

- [ ] Alpine 环境下 `1/2` 开关 BBRv3 可执行
- [ ] 非 Alpine 环境可正常进入外部 BBR 套件
- [ ] 返回上级时没有多余二次暂停

### 7. Docker管理

- [ ] 顶部状态统计正常显示（容器/镜像/网络/卷）
- [ ] `1` 安装/更新 Docker 可执行
- [ ] `2` 全局状态有版本/清单输出
- [ ] `3/4/5/6` 子菜单进入与返回正常
- [ ] `7/8/9/11/12/19/20` 有明确结果或确认提示

### 8. WARP管理

- [ ] 进入后会下载并拉起 WARP 菜单
- [ ] 下载失败时有明确失败输出，不应静默返回

### 9. 测试脚本合集（重点）

- [ ] `1~4` 解锁检测有输出或失败提示
- [ ] `11~18` 回程/测速脚本可执行
- [ ] `21/22/31/32/33` 至少抽测 3 项有有效输出
- [ ] `15` 指定 IP 输入后确实执行到目标脚本

### 10. 甲骨文云脚本合集

- [ ] `1/2` 活跃脚本安装与卸载可执行
- [ ] `3` DD 重装有明确风险确认
- [ ] `4/5/6` 执行类操作有明确过程输出

### 11. LDNMP建站

- [ ] 至少抽测 `1/21/23/31/38` 各 1 项
- [ ] 创建站点或反代后，Nginx 重载成功才提示成功
- [ ] 备份/恢复/卸载类有明确确认提示

### 12. 应用市场

- [ ] 应用列表完整显示
- [ ] 至少抽测 2 个应用可真正拉起安装/管理流程
- [ ] `b/r` 备份与还原入口有明确过程输出

### 13. 后台工作区

- [ ] `1~10` 固定工作区能进入/创建
- [ ] `21` SSH 常驻模式可开关
- [ ] `22/23/24` 创建、注入、删除工作区可执行

### 14. 系统工具

- [ ] 至少抽测 `1/6/12/15/17/20/31/34/99/102`
- [ ] 高风险项必须有确认提示
- [ ] 返回主菜单时不应出现多余二次暂停

### 15. 服务器集群控制

- [ ] 节点增删改可执行
- [ ] 批量命令有逐节点反馈
- [ ] `11~18/51` 至少抽测 2 项

### 99. 更新脚本

- [ ] Git 安装路径更新成功
- [ ] 非 Git 安装路径可走远程更新，且非空执行

### 88. 卸载脚本

- [ ] 执行后 `z` 命令不可用
- [ ] `/opt/luopo-toolkit` 被清理

### 0. 退出脚本

- [ ] 正常退出到 Shell

## 5. 当前结构状态备注

- [x] 主菜单 `5-15` 已全部接到 `modules/luopo/`
- [x] `modules/compat/` 已移除
- [x] `ldnmp` / `system_tools` 的模块级 `legacy_bridge.sh` 已移除
- [x] 活动模块不再调用 `ensure_luopo_vendor_loaded` / `run_luopo_compat_menu`
- [x] `vendor/luopo.sh` 仅作为本地来源参考和应急备份保留，不上传到 GitHub，也不作为活动运行依赖
- [x] 旧菜单草稿已改为本地保留，不再上传 GitHub：
  - `modules/menus/`
  - `modules/extended_menus.sh`
  - `modules/singbox.sh`

## 4. 自检结果模板（复制填写）

可复制以下模板，逐项标记并回传：

```text
[环境信息]
- 系统: Debian/Ubuntu + 版本
- 内核:
- 运行方式: z / z en
- 自检时间:

[结果汇总]
- 通过项:
- 失败项:
- 阻塞项:

[失败明细]
1) 菜单:
   选项:
   现象:
   复现步骤:
   终端输出:
   logs/action.log 片段:
   logs/error.log 片段:

2) 菜单:
   选项:
   现象:
   复现步骤:
   终端输出:
   logs/action.log 片段:
   logs/error.log 片段:
```

